# 🎯 Manga Shop System Flow Diagram
## Complete User Journey from Login to Order Completion

---

## 📊 MAIN USER FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         START: USER VISITS SITE                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Landing Page (/)            │
                    │   - Public Access             │
                    │   - Hero Section              │
                    │   - Features Display          │
                    └───────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  Click "Sign In"     │        │  Click "Sign Up"      │
        │  (Modal Opens)       │        │  (Modal Opens)        │
        └──────────────────────┘        └──────────────────────┘
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  Login Modal         │        │  Register Modal      │
        │  - Email             │        │  - Name              │
        │  - Password          │        │  - Email             │
        │  - Remember Me       │        │  - Password          │
        │                      │        │  - Confirm Password  │
        └──────────────────────┘        └──────────────────────┘
                    │                               │
                    │  (AJAX POST)                  │  (AJAX POST)
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  LoginController     │        │  RegisterController  │
        │  @login()            │        │  @register()         │
        └──────────────────────┘        └──────────────────────┘
                    │                               │
                    │  Validate Credentials         │  Validate & Create User
                    │                               │  - Hash Password
                    │                               │  - Set is_admin = false
                    │                               │  - Auto Login
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   ✅ AUTHENTICATED USER       │
                    │   Session Created             │
                    │   Redirect to /shop          │
                    └───────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 1: BROWSE PRODUCTS                             │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   GET /shop                  │
                    │   HomeController@index       │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Query Products             │
                    │   - Filter by Category       │
                    │   - Search by Name/Author    │
                    │   - is_active = true         │
                    │   - Paginate (12 per page)   │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Display Products Grid       │
                    │   - Product Cards             │
                    │   - Price, Stock, Image       │
                    │   - "View" & "Add to Cart"    │
                    └───────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  Click "View"        │        │  Click "Add to Cart"  │
        │  Product Details     │        │  (Quick Add)          │
        └──────────────────────┘        └──────────────────────┘
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  GET /products/{id}  │        │  POST /cart/add/{id}  │
        │  ProductController   │        │  CartController@add   │
        │  @show()             │        └──────────────────────┘
        └──────────────────────┘                    │
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  Product Detail Page  │        │  Validate Quantity    │
        │  - Full Description   │        │  Check Stock          │
        │  - Add to Cart Form   │        │  Get/Create Cart     │
        └──────────────────────┘        │  Add/Update Item      │
                    │                   └──────────────────────┘
                    │                               │
                    │  (User adds to cart)          │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 2: SHOPPING CART                               │
│                    (MAJOR FUNCTIONALITY #1)                             │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   GET /cart                   │
                    │   CartController@index        │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Load User's Cart            │
                    │   - Get Cart (1:1 with User)  │
                    │   - Load CartItems            │
                    │   - Load Products (Eager)      │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Display Cart Page           │
                    │   - Items Table               │
                    │   - Quantity Controls         │
                    │   - Remove Buttons            │
                    │   - Total Calculation         │
                    └───────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
        ┌───────────┴───────────┐       ┌───────────┴───────────┐
        │  Update Quantity      │       │  Remove Item          │
        │  PUT /cart/update/   │       │  DELETE /cart/remove/ │
        └───────────┬───────────┘       └───────────┬───────────┘
                    │                               │
                    ▼                               ▼
        ┌──────────────────────┐        ┌──────────────────────┐
        │  Validate Stock       │        │  Delete CartItem     │
        │  Update Quantity     │        │  Return to Cart       │
        │  Return to Cart       │        │                      │
        └──────────────────────┘        └──────────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   User Clicks                │
                    │   "Proceed to Checkout"      │
                    └───────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 3: CHECKOUT                                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   GET /checkout               │
                    │   OrderController@checkout    │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Validate Cart               │
                    │   - Not Empty?                │
                    │   - Stock Available?          │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Display Checkout Form       │
                    │   - Order Summary             │
                    │   - Shipping Form             │
                    │   - Address, City, etc.       │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   User Fills Shipping Info    │
                    │   Clicks "Place Order"        │
                    └───────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 4: PLACE ORDER                                 │
│                    (MAJOR FUNCTIONALITY #2)                             │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   POST /orders                │
                    │   OrderController@store       │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Validate Shipping Data      │
                    │   - Required Fields           │
                    │   - Format Validation         │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Re-check Stock              │
                    │   (Prevent Race Conditions)   │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Create Order                │
                    │   - Generate Order Number      │
                    │   - Calculate Total            │
                    │   - Set Status: "pending"      │
                    │   - Save Shipping Info         │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Loop Cart Items:             │
                    │   For Each Item:               │
                    │   1. Create OrderItem          │
                    │   2. Decrement Product Stock    │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Clear Cart                  │
                    │   - Delete All CartItems       │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Redirect to Order Details   │
                    │   GET /orders/{order}          │
                    └───────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 5: ORDER CONFIRMATION                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   OrderController@show       │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Display Order Details        │
                    │   - Order Number               │
                    │   - Items List                 │
                    │   - Shipping Address           │
                    │   - Total Amount               │
                    │   - Status: "pending"          │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Success Message:            │
                    │   "Order placed successfully!" │
                    └───────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    STEP 6: ORDER MANAGEMENT                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │   GET /orders                  │
                    │   OrderController@index        │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Load User's Orders          │
                    │   - Paginated List            │
                    │   - Eager Load Items          │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Display Orders List          │
                    │   - Order Numbers               │
                    │   - Dates                      │
                    │   - Statuses                   │
                    │   - Total Amounts               │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Click Order to View Details │
                    │   GET /orders/{order}         │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   Order Details Page          │
                    │   - Full Order Info           │
                    │   - Track Status              │
                    └───────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │   ✅ ORDER COMPLETE           │
                    │   Status Flow:                │
                    │   pending → paid →            │
                    │   shipped → completed          │
                    └───────────────────────────────┘
```

---

## 🔄 DATABASE OPERATIONS FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DATABASE INTERACTIONS                                │
└─────────────────────────────────────────────────────────────────────────┘

LOGIN/REGISTER:
    User Table
    ├── INSERT (Register)
    │   └── name, email, password (hashed), is_admin = false
    └── SELECT (Login)
        └── WHERE email = ? AND password matches

BROWSE PRODUCTS:
    Products Table
    └── SELECT * WHERE is_active = true
        ├── JOIN categories
        ├── Filter by category_id
        └── Search by name/description/author

ADD TO CART:
    Carts Table
    └── SELECT/INSERT (firstOrCreate)
        └── WHERE user_id = ?
    
    CartItems Table
    └── SELECT (check if exists)
        ├── UPDATE quantity (if exists)
        └── INSERT (if new)
            └── cart_id, product_id, quantity, price

VIEW CART:
    Carts Table
    └── SELECT WHERE user_id = ?
    
    CartItems Table
    └── SELECT WHERE cart_id = ?
        └── JOIN products
    
    Calculate: total_price = SUM(quantity * price)

PLACE ORDER:
    Orders Table
    └── INSERT
        ├── user_id
        ├── order_number (unique)
        ├── total_amount
        ├── status = 'pending'
        └── shipping_* fields
    
    OrderItems Table
    └── INSERT (for each cart item)
        ├── order_id
        ├── product_id
        ├── quantity
        ├── price
        └── subtotal
    
    Products Table
    └── UPDATE stock = stock - quantity
    
    CartItems Table
    └── DELETE WHERE cart_id = ?

VIEW ORDERS:
    Orders Table
    └── SELECT WHERE user_id = ?
        └── JOIN order_items
            └── JOIN products
```

---

## 🔐 AUTHENTICATION & AUTHORIZATION FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                                     │
└─────────────────────────────────────────────────────────────────────────┘

ROUTE PROTECTION:
    Public Routes (No Auth Required)
    ├── GET / (landing)
    └── GET /shop (browse)
    
    Guest Routes (Must NOT be logged in)
    ├── GET/POST /login
    └── GET/POST /register
    
    Auth Routes (Must be logged in)
    ├── POST /logout
    ├── GET /products/{id}
    ├── GET/POST /cart/*
    └── GET/POST /orders/*
    
    Admin Routes (Must be logged in AND is_admin = true)
    └── GET/POST /admin/*

MIDDLEWARE CHAIN:
    Request
    │
    ├──→ Guest Middleware
    │   └── If authenticated → redirect home
    │
    ├──→ Auth Middleware
    │   └── If not authenticated → redirect login
    │
    └──→ Admin Middleware
        └── If not admin → abort(403)

SESSION MANAGEMENT:
    Login Success
    ├── Create Session
    ├── Store User ID
    └── Regenerate Session ID
    
    Logout
    ├── Destroy Session
    ├── Invalidate Token
    └── Regenerate CSRF Token
```

---

## 📦 CART SYSTEM DETAILED FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CART OPERATIONS                                     │
└─────────────────────────────────────────────────────────────────────────┘

ADD TO CART:
    User Action: Click "Add to Cart" (quantity = 1)
    │
    ├──→ Validate Quantity (1 ≤ qty ≤ stock)
    │
    ├──→ Get User's Cart
    │   └── Cart::firstOrCreate(['user_id' => Auth::id()])
    │
    ├──→ Check if Product Already in Cart
    │   └── CartItem::where('cart_id', $cart->id)
    │       .where('product_id', $product->id)
    │
    ├──→ IF EXISTS:
    │   ├── New Quantity = Old Quantity + Requested Quantity
    │   ├── Validate: New Quantity ≤ Stock
    │   └── UPDATE cart_items SET quantity = New Quantity
    │
    └──→ IF NOT EXISTS:
        └── INSERT INTO cart_items
            (cart_id, product_id, quantity, price)
            VALUES ($cart->id, $product->id, $qty, $product->price)

UPDATE QUANTITY:
    User Action: Change quantity in cart
    │
    ├──→ Validate: 1 ≤ quantity ≤ product->stock
    │
    └──→ UPDATE cart_items SET quantity = $quantity
        WHERE id = $cartItem->id

REMOVE ITEM:
    User Action: Click "Remove"
    │
    └──→ DELETE FROM cart_items WHERE id = $cartItem->id

CLEAR CART:
    User Action: Click "Clear Cart"
    │
    └──→ DELETE FROM cart_items WHERE cart_id = $cart->id

CALCULATE TOTALS:
    Cart Model Accessor: getTotalPriceAttribute()
    │
    └──→ SELECT SUM(quantity * price) FROM cart_items
        WHERE cart_id = $this->id
```

---

## 🛒 ORDER SYSTEM DETAILED FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ORDER OPERATIONS                                    │
└─────────────────────────────────────────────────────────────────────────┘

CHECKOUT PROCESS:
    Step 1: Validate Cart
    ├── Cart exists?
    ├── Cart has items?
    └── All items have stock?
    
    Step 2: Display Form
    └── Show shipping information form
    
    Step 3: Submit Order
    ├── Validate shipping data
    ├── Re-validate stock (race condition protection)
    └── Proceed to order creation

ORDER CREATION:
    Transaction Start
    │
    ├──→ Create Order Record
    │   ├── Generate unique order_number: 'ORD-' + uniqid()
    │   ├── Calculate total_amount from cart
    │   ├── Set status = 'pending'
    │   └── Save shipping information
    │
    ├──→ Loop Through Cart Items
    │   │
    │   ├──→ Create OrderItem
    │   │   ├── order_id
    │   │   ├── product_id
    │   │   ├── quantity
    │   │   ├── price (snapshot from cart)
    │   │   └── subtotal = quantity * price
    │   │
    │   └──→ Update Product Stock
    │       └── product->stock = product->stock - quantity
    │
    └──→ Clear Cart
        └── DELETE all cart_items for this cart
    
    Transaction Commit

ORDER STATUS FLOW:
    pending (Initial)
    │
    ├──→ paid (Admin updates)
    │
    ├──→ shipped (Admin updates)
    │
    └──→ completed (Admin updates)
    
    OR
    
    └──→ cancelled (Admin/User cancels)

ORDER VIEWING:
    User Orders List
    ├── SELECT * FROM orders
    │   WHERE user_id = Auth::id()
    │   ORDER BY created_at DESC
    │   LIMIT 15 OFFSET (page * 15)
    │
    └── Eager Load: items.product
    
    Order Details
    ├── SELECT * FROM orders WHERE id = ?
    ├── SELECT * FROM order_items WHERE order_id = ?
    └── JOIN products ON order_items.product_id = products.id
```

---

## 👨‍💼 ADMIN FLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ADMIN OPERATIONS                                   │
└─────────────────────────────────────────────────────────────────────────┘

ADMIN DASHBOARD:
    GET /admin/dashboard
    │
    ├──→ Statistics
    │   ├── Total Orders Count
    │   ├── Pending Orders Count
    │   ├── Total Products Count
    │   └── Total Users Count
    │
    └──→ Recent Orders (Last 5)

PRODUCT MANAGEMENT (CRUD):
    CREATE:
    ├── GET /admin/products/create
    │   └── Show form with categories
    │
    └── POST /admin/products
        ├── Validate data
        ├── Generate slug from name
        └── INSERT INTO products
    
    READ:
    └── GET /admin/products
        └── SELECT * FROM products (paginated)
    
    UPDATE:
    ├── GET /admin/products/{id}/edit
    │   └── Show edit form
    │
    └── PUT /admin/products/{id}
        ├── Validate data
        └── UPDATE products SET ...
    
    DELETE:
    └── DELETE /admin/products/{id}
        └── Soft delete (deleted_at timestamp)

CATEGORY MANAGEMENT (CRUD):
    Similar to Products CRUD
    │
    └── DELETE Protection:
        └── Cannot delete if has products

ORDER MANAGEMENT:
    View All Orders:
    └── GET /admin/orders
        └── SELECT * FROM orders
            JOIN users
            JOIN order_items
            JOIN products
    
    Update Status:
    └── PUT /admin/orders/{id}/status
        ├── Validate status value
        └── UPDATE orders SET status = ?
```

---

## 🔄 COMPLETE REQUEST-RESPONSE CYCLE

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    REQUEST FLOW                                        │
└─────────────────────────────────────────────────────────────────────────┘

1. USER REQUEST
   └──→ HTTP Request (GET/POST/PUT/DELETE)
        ├── URL: /route
        ├── Method: GET/POST/PUT/DELETE
        ├── Headers: CSRF Token, Session
        └── Data: Form/Query Parameters

2. ROUTE MATCHING
   └──→ web.php
        ├── Match URL pattern
        ├── Check Middleware
        └── Resolve Controller@method

3. MIDDLEWARE EXECUTION
   └──→ Middleware Stack
        ├── Guest/Auth Check
        ├── Admin Check (if needed)
        └── CSRF Verification

4. CONTROLLER PROCESSING
   └──→ Controller Method
        ├── Validate Request
        ├── Business Logic
        └── Database Operations (Eloquent ORM)

5. DATABASE QUERY
   └──→ Eloquent ORM
        ├── Build Query
        ├── Execute SQL
        └── Return Model/Collection

6. RESPONSE GENERATION
   └──→ View Rendering
        ├── Load Blade Template
        ├── Pass Data
        └── Render HTML

7. HTTP RESPONSE
   └──→ Send to Browser
        ├── HTML Content
        ├── Status Code
        └── Headers
```

---

## 📊 DATA RELATIONSHIPS VISUAL

```
User (1) ──────< (1) Cart
 │                  │
 │                  │
 │                  └───< (Many) CartItems ────> (1) Product
 │
 │
 └───< (Many) Orders
      │
      └───< (Many) OrderItems ────> (1) Product

Category (1) ────< (Many) Products
```

---

## 🎯 KEY BUSINESS RULES

```
1. STOCK MANAGEMENT:
   - Cannot add to cart if quantity > stock
   - Stock validated at checkout
   - Stock decremented on order creation
   - Price snapshot at cart addition

2. CART RULES:
   - One cart per user (1:1 relationship)
   - Cart persists across sessions
   - Price locked at add-to-cart time

3. ORDER RULES:
   - Order number must be unique
   - Order total calculated from cart
   - Stock checked twice (cart + checkout)
   - Cart cleared after order creation

4. AUTHORIZATION:
   - Users can only view their own orders
   - Admins can view all orders
   - Only admins can manage products/categories
   - Only admins can update order status
```

---

## 🚀 END-TO-END EXAMPLE

```
USER JOURNEY: "John buys a manga"

1. John visits site → Landing Page
2. John clicks "Sign Up" → Modal opens
3. John registers → Account created, auto-login
4. John redirected to /shop → Browse products
5. John searches "Naruto" → Filtered results
6. John clicks "View" on product → Product details
7. John clicks "Add to Cart" (qty: 1) → Added to cart
8. John adds 2 more products → Cart has 3 items
9. John clicks cart icon → View cart page
10. John updates quantity of item 1 → Quantity updated
11. John clicks "Proceed to Checkout" → Checkout form
12. John fills shipping info → Form validated
13. John clicks "Place Order" → Order created
    ├── Order #ORD-ABC123 created
    ├── 3 OrderItems created
    ├── Stock decremented for 3 products
    └── Cart cleared
14. John redirected to order details → Order confirmation
15. John clicks "Orders" → See order in list
16. Admin updates status to "shipped" → Status updated
17. John views order → Sees "shipped" status
18. ✅ Order complete!
```

---

**END OF FLOW DIAGRAM**

